import 'package:flutter/cupertino.dart';

class ConfigLanguage {
  static const String langPath = "assets/languages";
  static const Locale enLocale = Locale('en', 'US');
  static const Locale arLocale = Locale('ar', 'KSA');
}
